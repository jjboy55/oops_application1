const bool yes =
    true; //a constant thats of the bool data type which equals true
const bool no =
    false; //a constant thats of the bool data type which equals true

//Testing equality
const doesOneEqualTwo = (1 ==
    2); // dart first infers the the type as a bool then it goes on to check the condition
//Testing inequality
const doesOneNotEqualTwo = (1 !=
    2); // dart first infers the the type as a bool then it goes on to check the condition
const bool alsoTrue = !(1 == 2);
